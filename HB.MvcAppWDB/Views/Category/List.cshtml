@{
    ViewData["Title"] = "List";
}

@model List<Kategori>


<table class="table">

    <thead>
        <tr>
            <th>ıd</th>
            <th>name</th>
        </tr>
    </thead>
    <tbody>
        @{

            foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                </tr>
            }
        }
    </tbody>
</table>
